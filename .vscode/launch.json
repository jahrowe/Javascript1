// launch.json
{
    "version": "0.2.0",
    "configurations": 
    [
        {
            "name": "Launch in Chrome",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
